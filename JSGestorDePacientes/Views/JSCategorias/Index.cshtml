@model IEnumerable<JSGestorDePacientes.Models.JSCategoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.JSGravedad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JSFechaIngreso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JSGravedad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JSFechaIngreso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.JSIdCategoria">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.JSIdCategoria">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.JSIdCategoria">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
